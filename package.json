{
  "name": "stud",
  "version": "1.0.0-preview.7",
  "description": "Local, agent-first 3D design workshop with material takeoffs and project budgets",
  "scripts": {
    "start": "python3 serve.py",
    "build": "python3 build.py",
    "test": "python3 -m unittest discover -s tests -v && node --test tests/*.mjs",
    "stud": "node bin/stud.js",
    "tauri": "tauri",
    "desktop:prepare": "node scripts/prepare-desktop.mjs",
    "desktop:build": "node scripts/build-desktop.mjs",
    "desktop:dev": "node scripts/build-desktop.mjs --dev",
    "desktop:smoke": "node scripts/smoke-desktop.mjs"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "three": "^0.180.0"
  },
  "private": true,
  "bin": {
    "stud": "bin/stud.js"
  },
  "devDependencies": {
    "@tauri-apps/cli": "2.11.4"
  }
}
